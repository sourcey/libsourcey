#ifndef PACKAGE_MANAGER_Packages_H
#define PACKAGE_MANAGER_Packages_H


#include "Sourcey/XML/XML.h"


namespace Sourcey { 
//namespace Anionu {



/*
	XML::Node latestAsset() const;
		// The latest update description set from the
		// web service response.
	
	virtual bool hasUpdates() const;
		// Returns true if updates are available.

	//void update(XML::Node manifest);
		// Updates the manifest data with the manifest
	
	bool save();
		// Saves the updated manifest file.

	bool valid() const;

	std::string path;
		// The local system path of the manifest file.

	std::string name() const;
	std::string version() const;
	//int revision() const;
*/


} // } // namespace Sourcey::Anionu


#endif // PACKAGE_MANAGER_Packages_H