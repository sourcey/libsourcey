add_subdirectory(deviceenumerator)

if(HAVE_FFMPEG)
  #add_subdirectory(audiosocket)
  add_subdirectory(videosocket)
  if(HAVE_OPENCV)
    if(BUILD_ALPHA)
      add_subdirectory(waveformoutput)
    endif()
  endif()
endif()
